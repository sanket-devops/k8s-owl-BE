apiVersion: v1
kind: Service
metadata:
  name: k8s-owl-be
  labels:
    name: k8s-owl-be
spec:
  ports:
    - port: 8008
      targetPort: http-port
      protocol: TCP
      nodePort: 30808
  externalIPs:
    - 192.168.120.135
  selector:
    app: k8s-be
  type: NodePort
